The files in this directory is a replica of what is put on gtweb-02.uit.no,
on the path /home/services/korp
They are settings files for the korp setup, as it is setup on gtweb2

corpus_configs/(smi/|nsu/|other/) - these are the settings for the korp-backend-\1
service. That is, for example:

korp-backend-smi is set up with the config folder /home/services/korp/corpus_configs/smi
